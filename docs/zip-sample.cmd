@echo off

cd vscode-workspace-sample
bestzip ../vscode-workspace-sample.zip ./

goto:eof

It's so tricky to remove extra nesting directory vscode-workspace-sample
bestzip installation
npm install bestzip
